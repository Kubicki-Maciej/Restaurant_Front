export const waiterOrder = (state={},action)=>{
    switch(action.type){
        case 'ADD_PRODUCT':
            return
        case 'removeProduct':
            return 
        case 'changeProductNumber':
            return
    }
};

export const screenMenuSelect = (state={}, action)=>{

}